export enum Gender {
  Male = 1,
  Female = 2
}

export enum UserType {
  User = 1,
  Manager = 2,
  Admin = 3
}

export enum OrderStatus {
  Open = 1,
  Confirmed = 2,
  Delivery = 3,
  Closed = 4,
  Deleted = 5
}

export enum ProductStatus {
  Active = 1,
  NotActive = 2,
  Deleted = 3
}
